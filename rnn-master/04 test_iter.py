"""
文件说明

"""
import numpy as np
from collections import defaultdict

# y_list = [1, 2, 4]
# input_val_arr = [np.random.random(5) for _ in y_list]
# print(input_val_arr)

# Q = (defaultdict(lambda: np.zeros(6)))
# print(Q)

